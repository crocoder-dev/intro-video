{{define "bubble"}}
.iv-bubble {
  position: absolute;
  width: auto;

  top: 30px;
  left: -40px;
  min-width: 60px;
  padding: 8px 14px;
  background: #fff;
  border: 2px solid rgb(79, 70, 229);
  border-radius: 10px;
  border-bottom-right-radius: 2px;
  text-align: center;
  box-shadow: 0 8px 16px 0 rgba(0, 0, 0, .2);
}
{{end}}
